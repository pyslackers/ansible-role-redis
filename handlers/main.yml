---
- name: enable redis
  systemd:
    daemon_reload: yes
    enabled: yes
    name: redis

- name: start redis
  service:
    name: redis
    state: started

- name: restart redis
  service:
    name: redis
    state: restarted
